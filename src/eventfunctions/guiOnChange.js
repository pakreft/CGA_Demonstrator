/**
 * Fires when the user changes the wind speed via the GUI. Called in setupGUI().
 * @param windSpeed
 */
export function onChangeWindSpeed(windSpeed) {}

/**
 * Fires when the user changes the wind direction via the GUI. Called in setupGUI().
 * @param windDirection
 */
export function onChangeWindDirection(windDirection) {}
